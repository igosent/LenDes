
  # Review App Link

  This is a code bundle for Review App Link. The original project is available at https://www.figma.com/design/0g4UGwGsSMw3y7Gsml3MoM/Review-App-Link.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
  run for openai -  node server/api/translate.ts  
